This repository contains the MATLAB code used to generate the dataset:

**"Tropical Cyclone Significant Wave Heights and Spatial Footprints"**

The workflow includes:
* Computation of significant wave height (Hs) fields using a parametric model
* Extraction of maximum wave height spatial footprints from time-evolving fields
* Automated processing of large datasets of tropical cyclones

The study focuses on tropical cyclones in the **North Atlantic basin (1980–2025)**.

## Methodology

### 1. Parametric Wave Model and Batch Processing (`PModel_fast_identical`)

The generation of significant wave height (Hs) fields is performed through a **fully automated batch-processing pipeline** based on IBTrACS tropical cyclone data.

#### Workflow

**Input data ingestion**
   * Reads cyclone tracks from IBTrACS (`IBTrACS_NA_1980_2025.csv`)
   * Extracts:
     * Position:
       * Latitude (degrees)
       * Longitude (degrees)
     * Maximum wind speed (`USA_WIND`, **knots → converted to m/s**)
     * Radius of maximum winds (`USA_RMW`, **nautical miles → converted to meters**)
     * Time (`ISO_TIME`, UTC)
     * Storm Identifier (`SID`, dimensionless)

**Storm-based processing**
   * Data are grouped by **Storm Identifier (SID)**
   * One output folder is created per cyclone:

     ```
     NAME_SID/
     ```

**Time-step iteration**
   For each time step:
   * Forward velocity (`Vfm`, **m/s**) is computed using the haversine distance
   * Wind speed is converted to **m/s**
   * Missing or invalid `RMW` values are replaced with a default (**30 km = 30,000 m**)
   * Derived parameter:
     * `R34` (**meters**)
   * Physical constraints are applied to all parameters

**Parallel execution**
   * Processing is parallelized using MATLAB `parfor`
   * Each time step is computed independently for efficiency

#### Wave Model

The function `PModel_fast_identical` computes the wave field using:

* Empirical formulation for:
  * Maximum significant wave height (**Hs_max, meters**)
* Multidimensional interpolation of precomputed wave diagrams
* Spatial transformation to geographic coordinates

**Important note**:
This implementation is a **modified and optimized version** of:

> Grossmann-Matheson, G., Young, I. R., Alves, J.-H., & Meucci, A.
> *Development and validation of a parametric tropical cyclone wave height prediction model*

#### Key Improvements

* Parallel processing (`parfor`) for large datasets
* Persistent memory caching of `.mat` files (major speed-up)
* Fully automated batch execution across all cyclones
* Robust handling of:

  * Missing data (`NaN`)
  * Inconsistent time formats
  * Longitude normalization (degrees)
* Direct CSV export per time step

#### Outputs

For each cyclone:

```
NAME_SID/
   ├── NAME_SID_P001.csv
   ├── NAME_SID_P002.csv
   └── ...
```

Each file contains:

```
ISO_Time (UTC) | Latitud (degrees) | Longitud (degrees) | Hs_m (meters)
```

#### Computational Details

* Precomputed wave fields are loaded from:

  ```
  Hsmax_diagrams/
  ```

* Interpolation is performed in a **4D parameter space**:

  * Wind speed `Vmax` (**m/s**)
  * Forward speed `Vfm` (**m/s**)
  * Radius `R34` (**km in interpolation space**)
  * Radius `Rmax` (**km in interpolation space**)

#### Spatial Output

* Wave fields are generated in geographic coordinates:
  * Latitude (**degrees**)
  * Longitude (**degrees**)
* Conversion assumptions:
  * 1° ≈ 111.1 km
* Output wave height:
  * `Hs` (**meters**)

#### Notes

* Results are sensitive to input cyclone parameters
* Default assumptions (e.g., missing RMW) may affect accuracy
* The model is intended for research applications

### 2. Footprint Extraction (Batch Processing Script)

This script performs **automated extraction of spatial footprints of maximum wave height** from the time series generated by the parametric model.

#### Workflow

For each tropical cyclone:

1. Reads all CSV files (`Hs_m`) corresponding to different time steps
2. Interpolates each time step onto a **fixed spatial grid**
3. Computes the **maximum wave height at each grid point** over time
4. Tracks the **time at which the maximum occurs**
5. Exports a final CSV containing the footprint

#### Key Features

* Fixed spatial grid:

```text
Longitude: -100 to -10  
Latitude:   0 to 50  
Resolution: 0.1°
```

* Robust handling of:
* 
  * Missing values (`NaN`)
  * Longitude conversion (0–360 → -180–180)
  * Time formatting inconsistencies
  * Year correction from malformed timestamps

* Automatic:

  * File sorting (P001, P002, …)
  * Skipping already processed storms
  * Error handling per cyclone

## Input Data

### Parametric Model

Requires:

```text
Hsmax_diagrams/
```
Containing `.mat` files used for interpolation.

### Footprint Processing

Input structure:

```text
PModel_Huracanes_X/
   ├── <SID>/
   │      ├── <SID>_P001.csv
   │      ├── <SID>_P002.csv
   │      └── ...
```

Each CSV contains:

```text
Latitud | Longitud | Hs_m | ISO_Time
```

## Outputs

### Parametric Model

```text
Latitud | Longitud | Hs_m
```

### Footprints

One file per cyclone:

```text
<SID>_Hs_max.csv
```
With variables:

```text
Tiempo   : timestamp of maximum wave height  
Latitud  : latitude (degrees)  
Longitud : longitude (degrees)  
Hs_max   : maximum significant wave height (m)
```

## Data Availability

Dataset available on Zenodo:
[DOI to be added after publication]

## Notes

* The model depends on precomputed wave diagrams.
* Footprints represent **maximum wave conditions during the cyclone lifecycle**.
* Results are sensitive to input parameter accuracy and model assumptions.

## Citation

If you use this code, please cite:

```text
Raventós, A. and Odériz, I. (2026). Tropical Cyclone Significant Wave Heights and Spatial Footprints.
```

and the original model:

```text
Grossmann-Matheson et al. (2023) Development and validation of a parametric tropical cyclone wave height prediction model.
```
## Funding

This work has been supported by the TRANSCILMA project.

## Contact

Anna Raventós
Universitat de Barcelona
[aravenro12@alumnes.ub.edu]

